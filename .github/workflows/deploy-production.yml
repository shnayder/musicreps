name: Deploy Production

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'docs/preview/**'

concurrency:
  group: production-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout main
        uses: actions/checkout@v4

      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Setup Node (for esbuild)
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Lint
        run: deno lint

      - name: Format check
        run: deno fmt --check

      - name: Type check
        run: deno check src/**/*.ts

      - name: Test
        run: deno test --no-check --allow-read

      - name: Build
        run: deno task build

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/index.html docs/design/
          if git diff --cached --quiet; then
            echo "No changes to docs/, skipping commit."
          else
            VERSION=$(grep -o 'v[0-9]\+\.[0-9]\+' docs/index.html | head -1)
            git commit -m "Build production: ${VERSION}"
            git push origin main
          fi
