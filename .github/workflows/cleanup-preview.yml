name: Cleanup Preview

on:
  delete:
  pull_request:
    types: [closed]

concurrency:
  group: gh-pages-deploy
  cancel-in-progress: false

jobs:
  cleanup:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    # Only run for claude/* or codex/* branches in this repo (not forks)
    if: >-
      (github.event_name == 'delete' && (startsWith(github.event.ref, 'claude/') || startsWith(github.event.ref, 'codex/'))) ||
      (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository && (startsWith(github.event.pull_request.head.ref, 'claude/') || startsWith(github.event.pull_request.head.ref, 'codex/')))
    steps:
      - name: Checkout (for deploy script)
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Cleanup preview
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            BRANCH="${{ github.event.pull_request.head.ref }}"
          else
            BRANCH="${{ github.event.ref }}"
          fi
          bash scripts/deploy-gh-pages.sh cleanup "$BRANCH"
