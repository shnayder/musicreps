[02/23/2026 11:16]

- web sandbox is convenient, but buggy. Hangs, needs bumping. 
- notifications are too noisy, not actionable enough -- session ids aren't helpful. Need human-readable names.
- too many interruptions, context switches.
    - reduce
    - provide better overview -- each session should have a name, should have a central place to see them all. Claude code desktop?
       - cleanup, status scripts? 
- manual UI testing
    - what's the next step in automation? Occassional bugs, but mostly design opinions which playwright et al won't catch.
- branch wrangling is a pain. Cleaning that up a bit -- remove build output from doc.
    - 
- I'm managing the review back and forth on PRs between claude, copilot
- unclear which worktree to use for what. 
    - dev process to eng?
    - features to product?
    - design process to design?
    -> loop is "work on feature/bug/etc -> retro -> improve". Keep in one workspace?
    -> Maybe just have them be red/green/yellow/blue workspaces, and use for whatever is top of mind? Could be three design features if that makes sense. 
- haven't been updating backlogs.
- git commands require approval when running locally. 


[02/25/2026 23:20]

The ui-iterate tool is already glorious. Tools for thinking!